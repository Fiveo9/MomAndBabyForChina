{
  "name": "mon",
  "type": "module",
  "private": true,
  "scripts": {
    "pdf": "node .vitepress/add-pdf-references.js",
    "dev": "pnpm run pdf && vitepress dev",
    "build": "pnpm run pdf && vitepress build",
    "preview": "vitepress preview"
  },
  "dependencies": {
    "vue": "^3.5.16"
  },
  "devDependencies": {
    "vitepress": "^1.0.0",
    "vitepress-plugin-lightbox": "^1.0.2"
  }
}
